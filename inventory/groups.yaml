---
# ------------------------------------------------------------
# Group-level defaults.
# Values here override defaults.yaml for group members.
# Host-level values take the highest precedence.
# ------------------------------------------------------------

spine:
  data:
    role_type: spine

leaf:
  data:
    role_type: leaf

core:
  platform: junos
  data:
    role_type: core

datacenter-a:
  data:
    datacenter: dc-a
    # Override credentials per datacenter if needed:
    # username: dc-a-user
    # password: dc-a-secret
