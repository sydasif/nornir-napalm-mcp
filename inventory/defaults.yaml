---
# ------------------------------------------------------------
# Global defaults — lowest precedence.
# Override at group or host level as needed.
# For production, replace plaintext credentials with:
#   - environment variables via nornir-env-transform
#   - a secrets manager plugin (HashiCorp Vault, etc.)
# ------------------------------------------------------------

username: admin
password: admin
port: 22
platform: eos
